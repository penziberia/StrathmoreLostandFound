<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ✅ Permissions OUTSIDE <application> -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <!-- Optional: If you want to indicate your app doesn't REQUIRE a physical camera -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        tools:targetApi="31">

        <!-- ✅ Launcher Activity -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- ✅ Home Screen -->
        <activity android:name=".HomeActivity" android:exported="true" />

        <!-- ✅ Post Item Screen -->
        <activity android:name=".PostItemActivity" android:exported="true" />

        <!-- ✅ Settings Screen -->
        <activity android:name=".SettingsActivity" android:exported="true" />

        <!-- ✅ Found Items Screen -->
        <activity android:name=".FoundItemsActivity" android:exported="true" />

    </application>

</manifest>
